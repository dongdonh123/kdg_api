<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kdg.api.mapper.board.CommunityBoardMapper">

    <resultMap id="BoardResultMap" type="com.kdg.api.model.board.Board">
        <id property="id" column="id"/>
        <result property="seq" column="seq"/>
        <result property="object_id" column="object_id"/>
    </resultMap>


    <select id="selectBoardList" resultMap="BoardResultMap">
        select
        board_id
        ,ROW_NUMBER() OVER (ORDER BY board_id) AS seq
        , (SELECT CEILING(COUNT(*)::numeric / 10) AS total_pages FROM kdh.tb_board ) as total_page
        ,#{page} as page
        FROM kdh.tb_board
        where 1=1
        order by board_id
        LIMIT 10
        OFFSET (#{page} - 1) * 10;
    </select>
</mapper>